import type { NextConfig } from "next";

// Todos os headers de segurança (incluindo CSP com nonce) são definidos
// em src/middleware.ts para que o nonce seja gerado por request.

const nextConfig: NextConfig = {};

export default nextConfig;
